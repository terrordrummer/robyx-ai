# Code Review Findings: Robyx

**Date**: 2026-04-16
**Baseline**: 1089 tests passed, 12,298 LOC
**Final**: 1085 tests passed (4 tests for dead code removed), 12,329 LOC

## Bug Findings (US1) — All Fixed

| ID | Module | Sev | Description | Fix |
|----|--------|-----|-------------|-----|
| F01 | updater.py | High | `extractall` without symlink/hardlink check | Added `issym()`/`islnk()` rejection |
| F02 | bot.py | Med | Double shutdown on signal + atexit | `_shutdown_done` guard |
| F03 | handlers.py | Med | `notes["min_compatible"]` when notes is None | None guard |
| F04 | updater.py | Med | Pip failure rollback skips data snapshot restore | Added restore to 3 paths |
| F05 | scheduler.py | Med | Reminder send catches only OSError/RuntimeError | Widened to `except Exception` |
| F06 | ai_invoke.py | Med | `read_text()` race after `stat()` | try/except with cache eviction |
| F07 | ai_invoke.py | Low | orphan_tracker in finally could skip cleanup | try/except/finally |
| F08 | handlers.py | Med | Double `get_by_thread` + side-effect pattern | Local variable |
| F09 | telegram.py | High | `_bot` not initialized in `__init__` | `self._bot = None` |
| F10 | topics.py | High | `platform=None` default causes AttributeError | Early None guard |
| F11 | orphan_tracker.py | High | `kept` dict never populated — clears registry | `kept[pid_str] = meta` |
| F15 | scheduled_delivery.py | Med | `read_text()` crash on non-UTF-8 | `errors="replace"` |
| F18 | config.py | Med | `int()` on env vars no ValueError guard | `_int_env()` helper |
| F19 | memory.py | Low | Double-brace placeholder mismatch | Fixed template |
| F21 | topics.py | Med | `close_workspace` crashes when platform is None | Guard added |

## Dead Code Removed (US2)

| Item | Module | What |
|------|--------|------|
| D1 | process.py | `is_bot_process()` async — never called |
| D7 | collaborative.py | `InteractionMode` enum — never used |
| D3-D5 | config.py | `ORCHESTRATOR_MD`, `SCHEDULER_MD`, `STATUS_INTERVAL` — never imported |
| D10 | topics.py | `_update_table_thread_id()`, `_update_specialist_thread_id()` — never called |

Additional dead items identified but kept (legacy fallback): memory.py legacy functions (save_active, append_archive, load_active, load_archive_index, word_count, is_over_budget).

## Security Findings (US3)

| ID | Module | Sev | Description | Fix |
|----|--------|-----|-------------|-----|
| S1 | handlers.py | Med | AI-controlled `work_dir` for continuous tasks not validated | Added WORKSPACE path validation |
| S2 | config_updates.py | Med | Security-critical keys (BOT_TOKEN, OWNER_ID) changeable via chat | Removed from KNOWN_ENV_KEYS |
| S3 | discord.py | Med | `download_voice` SSRF — fetches arbitrary URLs | Added domain validation |

**Confirmed clean**: command injection (no shell=True), token logging, privilege escalation, SQL injection, tarball extraction, image path validation.

## Performance Findings (US4) — Documented, Deferred

| ID | Module | Description | Status |
|----|--------|-------------|--------|
| P1 | scheduler.py | Redundant queue.json read in continuous task handler | Deferred — design change |
| P2 | scheduler.py | Blocking sync file I/O in async functions (os.fsync) | Deferred — needs asyncio.to_thread |
| P3 | scheduler.py | O(n) linear scan in reconciliation | Deferred — build index dict |
| P4 | agents.py | save_state() called too frequently | Deferred — needs debounce design |
| P5 | scheduler.py | Template re-read without cache | Deferred — trivial, low priority |

## Deferred Findings (need design decisions)

| ID | Module | Sev | Description |
|----|--------|-----|-------------|
| F12 | telegram.py | Med | `send_to_channel` unconditional Markdown — behavioral change risk |
| F13 | discord.py | Med | `download_voice` error handling incomplete |
| F14 | slack.py | Med | `reply`/`edit_message` no error handling |
| F17 | collaborative.py | Med | Partial load on malformed JSON — **closed by P2-30 (see Pass 2 Findings)** |
| F20 | voice.py | Med | `%` formatting can raise TypeError |
| All adapters | Med | `reply`/`edit_message` unprotected while `send_message` uses retry_send |

---

## Pass 2 Baseline (recorded 2026-04-16, T059)

| Metric | Value |
|--------|-------|
| Tests (pytest) | 1085 passed, 1 skipped (1086 collected) |
| LOC under `bot/` | 12 329 |
| Modules under `bot/` | 53 |
| Migration files (`bot/migrations/v*.py`) | 18 (`v0_20_12` … `v0_21_0`) — plan.md erroneously said 25 |
| Version | 0.21.0 |
| Plan-assumption correction | `bot/i18n.py` is **single-locale English**, not IT+EN. Pass 2 plan and conversation contract assumed bilingual — to correct before Phase 12. |

### Baseline refreshed 2026-04-18 (after rebase onto main, 003/004 merged via v0.22.1)

| Metric | Start (2026-04-16) | Refreshed (2026-04-18) | Delta |
|--------|--------------------|------------------------|-------|
| Tests (pytest) | 1086 collected | 1451 collected | +365 |
| LOC under `bot/` | 12 329 | 14 576 | +2 247 |
| Modules under `bot/` | 53 | 59 | +6 |
| Migration files | 18 (`v0_20_12` … `v0_21_0`) | 23 (`v0_20_12` … `v0_22_1`) | +5 (`v0_21_1`, `v0_21_2`, `v0_21_3`, `v0_22_0`, `v0_22_1`) |
| Version | 0.21.0 | 0.22.1 | +0.1.1 |
| New modules added | — | `bot/continuous_macro.py` (704 LOC), migrations `v0_22_0.py` + `v0_22_1.py`, + 5 new test modules | — |
| Audit targets modified by 003/004 | — | `bot/bot.py`, `bot/authorization.py`, `bot/messaging/base.py`, `bot/messaging/telegram.py`, `bot/messaging/discord.py`, `bot/messaging/slack.py`, `bot/collaborative.py`, `bot/handlers.py`, `bot/i18n.py`, `bot/ai_invoke.py`, `bot/scheduled_delivery.py` | — |

Close-out gate threshold (T117) correspondingly lifted from ≥ 1086 to ≥ 1451. New SEC task **T079a** added in `tasks.md` for `bot/continuous_macro.py`. **T112** (Pass 1 F17) marked closed — T085 already addressed it.

## Pass 2 Findings

| ID | Module | Lens | Sev | Description | Fix | Status |
|----|--------|------|-----|-------------|-----|--------|
| P2-00 | plan.md / contract | Meta | Low | Pass 2 planning docs claimed IT/EN locale parity, but `bot/i18n.py` is English-only | Corrected in plan.md + contracts/conversation-contract.md; single-locale discipline now articulated; multi-locale becomes a *future* concern | fixed |
| P2-01 | handlers.py:213 | NI | Low | Hard-coded literal `"Usage: /reset <name>"` passed to `platform.reply` (bypasses i18n) | Relocated to `STRINGS["reset_usage"]`; call site updated. | **fixed** |
| P2-02 | handlers.py:306 | NI | Low | Hard-coded literal `"Checking for pending update..."` passed to `platform.reply` | Relocated to `STRINGS["update_checking_manual"]`; call site updated. | **fixed** |
| P2-03 | handlers.py:1350 | NI | Low | Hard-coded literal `"No users registered in this workspace."` passed to `platform.reply` | Relocated to `STRINGS["collab_no_users"]`; call site updated. Ripgrep confirms zero remaining direct-literal violations across the 105 messaging call-sites. | **fixed** |
| P2-10 | messaging/slack.py:172 | Security | **High** | `download_voice` forwarded the bot token across 3xx redirects via `follow_redirects=True`, with no host allow-list on `file_id`. A hostile redirect could exfiltrate the Slack bot token. | Added `_validate_slack_file_url` guard (HTTPS + Slack-hostname allow-list). `download_voice` now disables automatic redirect following and re-validates every Location before replaying the Authorization header. | **fixed** |
| P2-20 | bot.py:108 | Stability | **High** | `ensure_single_instance` used a TOCTOU pattern (`if PID_FILE.exists(): read; else: write`). Two processes starting within the race window could both pass the check and both run — the single-instance promise was advisory at best. | Replaced with a POSIX `fcntl.LOCK_EX \| LOCK_NB` advisory lock on a sidecar `bot.pid.lock` file. The lock fd is held for the lifetime of the process; kernel releases it on exit (even on SIGKILL), so stale PID files no longer keep the lock held. Non-POSIX platforms fall back to the legacy check. | **fixed** |
| P2-11 | messaging/discord.py:170 | Security | Med | `download_voice` read the entire attachment body into memory via `resp.read()` without a size cap. A hostile redirect or crafted event could point the bot at a multi-GB payload and OOM the process. | Switched to streaming with `resp.content.iter_chunked(64 KB)`, capped at `_MAX_DISCORD_DOWNLOAD_BYTES = 25 MB`. Content-Length header also short-circuited when it declares > 25 MB. | **fixed** |
| P2-12 | messaging/discord.py:159 | Security | Med | The HTTPS + Discord-hostname allow-list from Pass 1 (S3) was inline in `download_voice` only. Any future download path would have to re-implement it, risking an SSRF regression. | Factored into module-level `_validate_discord_url()` with the allow-list expanded to include `discordapp.net` (Discord's media CDN). Called from every HTTP fetch path. | **fixed** |
| P2-30 | agents.py:151 + collaborative.py:170 | Stability | Med | A malformed `state.json` or `collaborative_workspaces.json` triggered a broad `except` that logged a warning and continued with empty in-memory state. The NEXT write would overwrite the corrupt file, destroying the original bytes forever. | Both load paths now: (a) catch `json.JSONDecodeError` and `UnicodeDecodeError` separately from other exceptions, (b) call new `_quarantine_corrupt_file(path, reason)` to rename the bad file to `*.corrupt-<UTC-timestamp>` before continuing. Operators keep the forensic evidence; the next save creates a fresh file. **Closes Pass 1 F17.** | **fixed** |
| T072 | messaging/slack.py | Security | — | Follow-up items for Slack adapter: event-ID dedup store bound + error-path token scrubbing | **Re-scoped to 'no action'**: event dedup is handled by `slack-bolt` library (not our code); `_bot_token` only appears in the `download_voice` Authorization header, never echoed in logs. No residual gap. | noted |
| P2-40 | migrations/tracker.py:51 | Stability | Med | `save(data_dir, tracker)` used plain `path.write_text(json.dumps(...))` with no tmp-file, no `fsync`, no atomic rename. A SIGKILL or power loss mid-write could leave `migrations.json` partially serialised — on next boot `load()` treats it as empty and **every migration in the chain re-runs** (safe only if every step is strictly idempotent). | Rewritten to go through `tmp + fsync + os.replace`. `fsync` failures are swallowed (tmpfs / non-durable filesystems) but the rename is still atomic. Test simulates a replace-step failure and verifies the original file is byte-identical afterwards. Closes C2 from `crash-matrix.md`. | **fixed** |
| T074 | config_updates.py | Security | — | Trust-boundary X-3: `.env` hot-reload should be guarded by a mutex so secrets don't rotate mid-AI-invocation | **Re-scoped to 'no action'**: there is **no hot-reload mechanism** in the codebase. `config_updates.py` writes `.env`; the bot reads it once at startup via `load_dotenv()`. User-facing i18n strings (`voice_no_key`) explicitly tell users to restart the bot after editing `.env`. Trust-boundary X-3 was mis-identified. | noted |
| T080 | scheduler.py | Stability | Med | Crash-matrix C6: scheduler intervals use wall-clock; an NTP jump can cause double-fire or missed-fire of retries and stale-claim resets | **Deferred with rationale**: after reading the code, `scheduler.py` uses `datetime.now(timezone.utc)` (not `time.time()`) and **most usages are legitimately wall-clock** — serialised `created_at`/`sent_at`/`canceled_at` timestamps, absolute `fire_at` deadlines, recurring-task `run_at` advancement. A correct fix would require threading monotonic references through in-memory state for interval-only computations (stale-claim TTL, reminder-age checks) while keeping wall-clock for serialisation and user-visible deadlines — substantially larger than one slice. Needs its own spec. | deferred |
| P2-50 | media.py:71 | Security | Med | Pillow's default `MAX_IMAGE_PIXELS` (89 MP) raises `DecompressionBombWarning` — NOT an error, so a crafted image in that range would still be decoded. No pre-Pillow file-size cap either, so a 10 GB crafted image would reach the decoder before any cap kicked in. Trust-boundary TG-4. | Added `_MAX_IMAGE_FILE_BYTES = 25 MB` on-disk ceiling enforced before `Image.open`; lowered `Image.MAX_IMAGE_PIXELS` to 50 MP; wrapped the open/load in `warnings.catch_warnings()` + `simplefilter('error', DecompressionBombWarning)` so any suspicious image raises `MediaError` instead of silently allocating. Handles both the `DecompressionBombWarning` warning zone and the hard `DecompressionBombError` (`> 2 × MAX_IMAGE_PIXELS`). | **fixed** |
| P2-60 | i18n/handler parity | NI | Low | No automated test verified that every registered `/command` appears in `help_text` and vice versa. Similarly, no test caught unsubstituted `{placeholder}` style tokens (Pass 1 F19 had flagged one such case in `memory.py`). A future handler addition could ship an undiscoverable command or a help entry pointing to a non-existent handler. | Added `tests/test_i18n_parity.py` with two test classes: `TestStringSubstitution` (parametrised over every `STRINGS` key — verifies `%s`/`%d` substitution produces a clean output with no leftover format specs, and no `{name}` style placeholder survives) and `TestHelpParity` (`handler.keys()` ⟷ `help_text` set-equality across registered commands). +148 parametrised tests. | **fixed** |
| P2-70 | ai_invoke.py:431 | Security | Med | The AI CLI subprocess was spawned without an explicit `env=` argument, so it inherited the full parent environment — including `ROBYX_BOT_TOKEN`, `DISCORD_BOT_TOKEN`, `SLACK_BOT_TOKEN`, `SLACK_APP_TOKEN`. A crash dump from the CLI that echoed env, or a prompt-injected agent that read `os.environ`, would surface bot secrets the CLI never needed. Trust-boundary X-1. | Added `_scrubbed_child_env()` helper that returns a copy of `os.environ` with `_SCRUBBED_ENV_KEYS` removed (the five bot-platform tokens + the legacy `KAELOPS_BOT_TOKEN` alias). Used as `env=` on `create_subprocess_exec`. Denylist (not allowlist) so user-set env like `HTTP_PROXY`, `LANG`, and provider API keys (`OPENAI_API_KEY`, `ANTHROPIC_API_KEY`) continue to pass through. +7 regression tests. | **fixed** |
| T065 | handlers.py | Security | — | Security audit against `trust-boundaries.md` (auth-before-mutation, AI-controlled path validation, command-injection residuals, secret-in-error-reply) | **Audit clean**: `owner_only` applied to every `cmd_*` and gated before the collab bypass in `handle_message`; Pass 1 `WORKSPACE` allow-list (S1) + SEND_IMAGE allow-list (S2) still in place; no shell injection primitives; `str(e)` echoed to users is all controlled `ValueError` from `create_workspace`/`create_specialist`. One marginal case at line 485 for generic `_process_and_send` exceptions flagged as low-priority hardening — not a verified gap given the AI CLI tools we support don't echo secrets. | noted |
| P2-71 | updater.py:689 (pip) + updater.py:648 (migrations) | Security | Med | `pip install` and migration-step subprocesses were spawned without an explicit `env=`, so they inherited the full parent environment including platform tokens (`ROBYX_BOT_TOKEN`, `DISCORD_BOT_TOKEN`, `SLACK_BOT_TOKEN`, `SLACK_APP_TOKEN`, `KAELOPS_BOT_TOKEN`) and AI provider keys (`OPENAI_API_KEY`, `ANTHROPIC_API_KEY`). A hostile `setup.py` in a transitive dep, a compromised wheel that execs during install, or a `PIP_INDEX_URL`-redirected proxy could read those secrets. | Added `_scrubbed_child_env()` helper in `bot/updater.py` (same pattern as P2-70 / `ai_invoke.py` but with a broader scrub list that also drops AI provider keys — pip and migrations don't need them). Wired into both `create_subprocess_exec` call sites via `env=_scrubbed_child_env()`. Operator-set env (`PATH`, `HOME`, `PIP_INDEX_URL`, proxies) passes through unchanged. +5 regression tests (unit-level on the scrub helper + integration spies on apply_update). | **fixed** |
| P2-72 | updater.py:151 (_restore_data_dir) | Security | Low | The snapshot-restore path validated member types (symlink/hardlink rejected by Pass 1 F01) and paths (absolute + `..` traversal rejected post-F01), but did NOT cap cumulative uncompressed size. A maliciously-swapped snapshot under `DATA_DIR/backups/` (or an accidentally-huge legitimate one) would extract to disk until the filesystem filled. Same class as P2-11 on discord.py. Defense-in-depth: an attacker with write access to `DATA_DIR/backups/` can already do worse, but a bounded restore is cheap and prevents accidental disk-fill during rollback. | Added module-level `MAX_RESTORE_TOTAL_BYTES = 5 GiB` and a running-sum check in the member-validation loop. First member that pushes cumulative `member.size` past the cap fails the restore with a logged error. +1 regression test (monkeypatches the cap to 0 and asserts a normally-valid snapshot is refused). | **fixed** |
| T067 | updater.py | Security | — | Full Pass 2 security audit: re-verify Pass 1 tarball rejection, add size caps, validate pip subprocess env, confirm rollback doesn't widen exec permissions | **Re-verified clean**: F01 symlink/hardlink rejection still in place (extended post-F01 with absolute-path + `..` + `resolve().relative_to()` checks); F04 rollback→restore invoked in all 4 failure paths (migration fail, pip timeout, pip non-zero, smoke fail); no `shell=True` anywhere; rollback does NOT widen executable permissions (git restores code file modes from tag; tar preserves data-dir modes). **New findings closed**: P2-71 (pip/migration env leak) + P2-72 (restore size cap). **Deferred (noted, no action)**: `_git()` also inherits env — git doesn't need tokens either, but it reads the repo's own URLs and writes inside the repo; scrubbing is low-value here. `restart_service()` Popen calls to `launchctl`/`systemctl`/`powershell` also inherit env — system commands, negligible surface. | fixed |
| T068 | bot.py | Security | — | Pass 2 audit of startup/shutdown: PID-lock race window, signal handler secret-echo, atexit ordering leaks. File was modified by 003/004 (+93 lines, new ChatMemberHandler for collab workspace membership events) — re-audited current state. | **Audit clean**. (1) PID-lock race window: already closed by P2-20 (T081) — `ensure_single_instance()` uses `fcntl.flock(LOCK_EX\|LOCK_NB)` on sidecar `bot.pid.lock`, kernel-held for process lifetime, no TOCTOU. Covered by 7 tests in `TestEnsureSingleInstance`. Windows fallback remains TOCTOU-susceptible but accepted — Windows service manager prevents concurrent starts in practice. (2) Signal handler secret-echo: SIGTERM/SIGINT handlers call `save_on_exit()` which logs a fixed "Shutting down" string and calls `manager.save_state()` (JSON write, no token interpolation). No env/token/traceback echoed. (3) atexit ordering: only one handler registered (`save_on_exit`), idempotent via `_shutdown_done` flag. No inter-module atexit ordering dependencies (grep confirms no other `atexit.register` in `bot/`). (4) 003/004 additions (ChatMemberHandler dispatch) are scoped to handler routing, do NOT touch lock/signal/atexit paths; new log lines use `%r` on user-controlled `chat.title` — injection-safe. No new findings, no code changes required. | noted |
| T069 | scheduler.py | Security | — | Pass 2 audit of attacker-controllable queue fields, path-traversal in `work_dir`, retry amplification DoS. | **Audit clean**. (1) Queue field validation: `task["name"]` validated via `validate_task_name` at every claim (lines 199, 336, 793, 910 — single path segment, no newlines/pipes/control chars, rejects `.`/`..`); `task["agent_file"]` validated via `resolve_agent_file_path` against the 2-segment `agents/<name>.md` or `specialists/<name>.md` pattern only. Other queue fields (`type`, `scheduled_at`, `interval_seconds`, `model`, `thread_id`) are enumerated strings or parsed as timestamps (`_parse_timestamp` raises `ValueError` on garbage). (2) `work_dir` path-traversal: `resolve_task_runtime_context` pulls `work_dir` from the Agent record in `state.json`, NOT from the queue entry directly. Every `manager.add_agent(...)` call site I traced (`handlers.py:954/1010/1878`, `topics.py:144/389/461/538`) passes `work_dir=str(WORKSPACE)` from config. The AI macro parsers (`CREATE_WORKSPACE_PATTERN`, `CREATE_SPECIALIST_PATTERN`) do NOT capture a `work_dir` group — the AI cannot inject a path. (3) Retry amplification: reminders capped by `MAX_REMINDER_ATTEMPTS` + `REMINDER_MAX_AGE_SECONDS` + `CLAIM_TIMEOUT_SECONDS`; tasks advance `next_run` monotonically, no unbounded retry loop. **Low-priority defense-in-depth observation** (noted, no action): `agent.work_dir` is not re-validated against the WORKSPACE allow-list at state.json load time. Current protection is "state.json is written only by us" — sufficient for the current threat model but worth revisiting if state.json ever becomes shared/untrusted storage. | noted |
| P2-73 | continuous_macro.py:462 | Security | Low | `apply_continuous_macros` fed `tok.program_raw` straight into `json.loads` with no size cap. A prompt-injected or malfunctioning AI could emit a multi-megabyte `[CONTINUOUS_PROGRAM]` body (or a deeply-nested JSON bomb); parsing burns CPU/RAM before the caught `ValueError` short-circuits. Same class as P2-11 (Discord download) / P2-72 (snapshot restore) — bound an AI-controlled input at the point of parse. | Added module-level `_MAX_PROGRAM_BYTES = 64 KiB` (~100× any realistic program). Size check runs on the UTF-8-encoded payload **before** `json.loads`. Oversize payloads are rejected with `reason="bad_json"` + `detail="program payload exceeds N-byte cap"`, matching the existing malformed-JSON failure mode (macro still stripped unconditionally so the payload never leaks to the user). +2 regression tests (oversize → bad_json, at-cap-valid → intercepted). | **fixed** |
| T079a | continuous_macro.py | Security | — | Pass 2 audit of the new 704-LOC `bot/continuous_macro.py` module introduced by feature 004: regex-DoS resistance, JSON size/depth limits, work_dir path-traversal / WORKSPACE allow-list enforcement, schema-field sanitization before dispatch, idempotent parse→reject→quarantine path. | **Re-verified clean**: (1) Regexes use negated character classes + non-greedy literal-delimited `.*?` — no nested quantifiers, no catastrophic-backtracking risk. (2) JSON size cap added (P2-73 above). Depth cap is implicit in Python's recursion limit; caught by the existing `except (ValueError, TypeError)`. (3) `work_dir` confined via `Path(work_dir).resolve().relative_to(workspace_root)` at line 519 — classic safe pattern, rejects absolute paths outside WORKSPACE and symlink-escapes that `resolve()` normalises. (4) Schema: `_first_missing_field` enforces `objective`/`success_criteria`/`first_step` presence, type-checks `objective` (str), `success_criteria` (non-empty list) before passing to `create_ws`. (5) Idempotency: stripping is unconditional (comment in module docstring: "partial or malformed match is scrubbed so the failure mode is 'not executed', never 'leaked'"); every rejection path appends to `outcomes` and `continue`s (no early exit that could leak a partial side effect). Cross-referenced against `specs/004-fix-continuous-task-macro/contracts/continuous-macro-grammar.md` — all grammar invariants enforced. | fixed |
| P2-80 | authorization.py:105 (`is_authorised_adder`) | Security | Med | `is_authorised_adder` iterated `collab_store.list_all()` — which includes `status="closed"` workspaces whose role maps survive closure — and treated any OWNER/OPERATOR hit as authorisation to add the bot to a NEW Telegram group. That conflicted with the operator-visible trust semantics of `/close` (the only mechanism an owner has to revoke an operator's provisioning right for a given workspace). An ex-operator of a closed workspace would keep the ability to drag the bot into arbitrary new groups even after the owner deliberately closed the shared context. Reachable via Flow B (bot added by non-owner) when the owner had previously closed a workspace that user operated. | Added module-level `_ACTIVE_STATUSES = {"active", "setup", "pending"}` allow-list; `is_authorised_adder` now skips workspaces whose `status` is not in that set before inspecting the role map. `pending` and `setup` are retained because they represent in-flight trust (a creator pre-announcing a workspace, or mid-bootstrap) — dropping them would break Flow A / Flow B intentional provisioning. +4 regression tests covering: closed-workspace operator rejected, closed-workspace owner rejected (role-based), setup/pending workspaces still grant rights, mixed active+closed uses the active-membership path. | **fixed** |
| T078 | authorization.py | Security | — | Pass 2 audit of the permission-check module modified by 003 (new `is_authorised_adder` helper, new collaborative call sites): every permission check reachable, no bypass via unset field, owner-elevation correctness, collab auth path trust envelope. | **Audit clean apart from P2-80 (fixed above)**. (1) `get_user_role`: fail-closed when `owner_id is None`; HQ vs. collab chat correctly distinguished via `collab_store.get_by_chat_id`; owner elevation (`Role.OWNER, ws`) covers collab groups so `can_manage_roles(OWNER)` works even when the owner isn't enrolled in the workspace's role map. (2) `can_send_executive`: total function, `None → False`. (3) `can_close_workspace`: identity-based (ws.created_by OR owner_id); the `role` arg is intentionally unused (documented in docstring) — prevents role-promotion from granting closure rights. Guards `owner_id is not None` before match. (4) `can_manage_roles`: OWNER-only; used for `/promote`, `/demote`, `/mode`. (5) `is_authorised_adder`: now scoped to non-closed workspaces (P2-80 fix). **Adjacent observation filed as P2-81 (deferred, not fixed in this commit to keep T078 diff scoped to authorization.py)**: the `[COLLAB_ANNOUNCE name="…"]` attribute is AI-emitted and passes through `_handle_collab_announce` at `handlers.py:607` into `AGENTS_DIR / (f"{name}.md")` with no name validator. Flow B already sanitises via `re.sub(r'[^a-z0-9-]', ...)`; Flow A does not. Needs a separate fix + test. | **audit clean (P2-80 closed; P2-81 filed)** |
| P2-81 | handlers.py:607 (`_handle_collab_announce`) + collaborative.py:352 (`create_pending`) | Security | Med | The `name` attribute inside `[COLLAB_ANNOUNCE name="…" purpose="…"]` is AI-emitted. `_handle_collab_announce` composes `agent_file = AGENTS_DIR / ("%s.md" % name)` and `agent_file.write_text(...)` BEFORE any sanitization — so a name like `../../evil` would write (AI-controllable) markdown content outside `AGENTS_DIR`. `create_pending` only rejects blank/colliding names, not traversal. Reachable via AI prompt injection: a collab-group participant sends text that is forwarded to HQ via `[NOTIFY_HQ]` and, if included in a subsequent Robyx-orchestrator turn, influences Robyx to emit a crafted `[COLLAB_ANNOUNCE]`. Flow B ad-hoc setup already sanitises via `_re.sub(r'[^a-z0-9-]', …)` at `handlers.py:1849`; Flow A does not. | **fixed 2026-04-19 via T078a close-out commit**. Added module-level `_VALID_COLLAB_NAME_RE = re.compile(r"^[a-z0-9][a-z0-9-]{0,63}$")` + `validate_collab_name(name)` in `bot/collaborative.py`. Called from two places: (1) `handlers._handle_collab_announce` BEFORE `agent_file.write_text(...)`, rejected values surface via the existing `collab_announce_error` STRING (`"invalid name: …"`) so the orchestrator sees the rejection in its next turn; (2) `CollabStore.create_pending` as defense-in-depth in case a future caller bypasses the handler layer. Validator matches the alphabet Flow B already produces via `_sanitize_task_name`, so tightening Flow A does NOT reject any legitimate Flow-B name. +39 parametrised regression tests across `tests/test_collaborative.py` (19-case validator matrix + 6-case `create_pending` rejection matrix) and `tests/test_collab_announce_command.py` (13-case handler-level rejection matrix verifying zero agent-file residue + no pending record + invalid-name trailer). Cross-checked: traversal names are rejected, mixed case rejected, length > 64 rejected, control bytes rejected, all legitimate names still accepted. | **fixed** |
| P2-82 | messaging/telegram.py:175 (`download_voice`) | Security | Med | `download_voice` called `voice_file.download_to_drive(tmp_path)` with no size ceiling: a single voice message up to Telegram's own 50 MB cap, or a sustained flood of near-cap messages, could fill operator disk before any downstream size check fired. Same defense-in-depth class as P2-11 (Discord streaming cap) / P2-72 (snapshot restore cap): bound an attacker-controllable input at the point of ingestion. Token-leak review: `_api_base` contains the bot token in the URL path, so ANY httpx exception whose `__str__` exposes the request URL would echo the token. In practice, our code never calls `raise_for_status`, and the exception types we log (`ConnectError`, `ReadTimeout`, `TransportError`) do not include the URL in `str(exc)`. No actionable user-visible leak in current code paths; left as a low-priority architectural observation — see T070 close-out note below. Filename-path-traversal review: `tempfile.NamedTemporaryFile(suffix=".ogg", delete=False)` writes under the OS temp dir with a random name, no caller-controlled segment. Clean. | Added module-level `_MAX_TELEGRAM_VOICE_BYTES = 25 MB` and a two-step guard in `download_voice`: (a) reject BEFORE any download when `voice_file.file_size` (declared by Telegram's `getFile` response) exceeds the cap, so oversize files touch zero bytes on disk; (b) verify actual bytes after download against the cap as defense-in-depth against a server lying between `getFile` and the download endpoint. Both failure paths unlink the temp file. Any download-time exception (network drop, PTB error) also unlinks before re-raising — no stale `.ogg` residue on rejection. +5 regression tests: declared-oversize → refused with no download, at-cap-valid → accepted, server-lies-post-download → caught + cleaned, missing `file_size` attribute → falls through to post-download guard without crash, transport exception → temp unlinked. | **fixed** |
| T070 | messaging/telegram.py | Security | — | Pass 2 audit of the Telegram adapter (trust-boundaries TG-1 voice download, TG-2 attachment path, TG-3 outgoing message-length propagation, and the token-in-URL-path leak class): size caps before download, filename path-traversal, token never in error replies, message-length bounds propagated. File carried a 003/004 drift marker (+15 lines for the `leave_chat`, `get_invite_link`, `bot_username`, `rename_main_channel` additions). | **Audit clean apart from P2-82 (fixed above)**. (1) `download_voice`: size cap closed via P2-82. (2) `send_photo`: `path` comes pre-validated against WORKSPACE by `_handle_media_commands` (T065); compression goes through `media.prepare_image_for_upload` which already has its own size cap (P2-50). Temp-file cleanup in `finally`. (3) `max_message_length = 4000` leaves a 96-byte safety margin under Telegram's 4096 cap; splitter in handlers.py uses it. (4) Error replies: `send_message` surfaces `result["description"]` (Telegram-produced, no token) via `RuntimeError`; other methods catch + log + return `None`/`False` without propagating URL-bearing exceptions to the user. (5) `leave_chat` / `get_invite_link` / `rename_main_channel` (new from 003) use PTB high-level methods that do not echo the token on error. **Low-priority architectural observation (noted, no action)**: the Bot API encodes the bot token into the URL path (`https://api.telegram.org/bot<TOKEN>/method`), so any future code path that calls `resp.raise_for_status()` OR surfaces a `URL`-bearing exception message to users would leak the token. Currently the adapter never calls `raise_for_status` and every httpx call either consumes `resp.json()` or catches + logs internally, so the leak is theoretical. A future hardening could move the token to an Authorization-style construction, but that is an architectural change beyond the audit scope. | fixed |
| T073 | messaging/base.py (+ telegram/discord/slack) | Security | — | Pass 2 audit of the `Platform` ABC contract after 003/004 extended it by four hooks (`leave_chat`, `get_invite_link`, `bot_username`, `rename_main_channel`): confirm every abstract method is implemented by all three adapters and the new hooks are wired uniformly (or explicitly refused with a clear rationale). File carried a 003/004 drift marker (+13 lines). | **Audit clean, no new findings**. (1) Every `@abc.abstractmethod` in `base.py` (`max_message_length`, `control_room_id`, `is_owner`, `is_main_thread`, `reply`, `edit_message`, `send_message`, `send_typing`, `send_photo`, `download_voice`, `create_channel`, `close_channel`, `send_to_channel`, `rename_main_channel`) is implemented by all three adapters. `rename_main_channel` (added during migration framework work, reinforced by 003) is present in `telegram.py:270`, `discord.py` (mocked-in-tests line), and `slack.py` with matching signature. (2) `leave_chat`: Telegram implements via PTB (`telegram.py:255`); Discord (`discord.py:358`) and Slack (`slack.py:292`) explicitly raise `NotImplementedError` with a platform-specific message ("external collaborative groups are Telegram-only in this iteration"). The explicit overrides are redundant with the base default (which also raises `NotImplementedError`) but improve the log message — not a gap, just a style choice. Per FR-013 / lifecycle-events.md the collab handler already tolerates `NotImplementedError` and logs a warning without surfacing to the user. (3) `get_invite_link`: only Telegram overrides (`telegram.py:238`); Discord and Slack inherit the default `return None`. Consistent with the Telegram-only collab scope and verified safe: `handlers.py:1788/1917` only invokes this from the `collab_bot_added` code path, which itself fires only on Telegram (Discord/Slack emit `collab.unsupported_platform` events and exit early). (4) `bot_username`: Telegram overrides (`telegram.py:297`); Discord and Slack inherit the default `None`. This is load-bearing for passive-mode mention detection in `handlers.py:1519-1525` — a `None` value forces `mentioned=False`, and non-executive non-mention senders are then silenced. That is MORE restrictive on non-Telegram platforms, so fail-closed and acceptable; the dependency is noted but not a finding. (5) Signature parity: `chat_id` / `thread_id` typed as concrete `int` in Telegram/Discord and `Any` in Slack (Slack ids are strings). ABC uses `Any`, so overrides are covariance-consistent. `send_message` error behaviour is pre-existing inconsistency (Discord returns `None` on missing channel, others raise) — out of T073 scope and already flagged by Pass 1 F14. **No code changes required; no P2-NN finding filed.** | noted |
| P2-83 | voice.py:21 (`transcribe_voice`) + handlers.py:1323 (voice handler) | Security | Med | `transcribe_voice` shipped the local voice file to OpenAI's Whisper endpoint with no pre-flight size check. Whisper's own hard cap is 25 MB per request — oversize files return 413 after a slow round trip that ties up the httpx client + `VOICE_TIMEOUT`-second window. Adjacent gap in the caller: `handlers.py:1326` called `os.unlink(tmp_path)` unconditionally AFTER awaiting `transcribe_voice`, so an `asyncio.CancelledError` mid-transcription (shutdown, typing-race cancellation) or any uncaught exception class outside the 4 types `transcribe_voice` catches would leak the `.ogg` tempfile in the OS temp dir across process lifetimes. Upstream size caps (P2-82 Telegram, P2-11 Discord) already bound legitimate traffic, but a defense-in-depth cap at the point of use was missing — and Slack `download_voice` still has no cap (noted, out of T076 scope; would belong to T072 follow-up). Token-handling re-verified clean: OpenAI key only used in `Authorization: Bearer …` header; neither `log.error("Whisper error: %s", response.text[:200])` nor `log.error("Transcription error: %s", e)` can echo the header (OpenAI's response bodies carry error JSON, not our headers; httpx exceptions do not stringify request headers). | (1) Added module-level `_MAX_TRANSCRIPTION_BYTES = 25 MB`. `transcribe_voice` now calls `os.path.getsize` up-front and short-circuits with a new i18n string `voice_too_large` (declares the file size + cap in MB) if the file exceeds the ceiling — zero network bytes spent on oversize input. `os.path.getsize` errors are surfaced via the existing `voice_error` string (action: retry). (2) `handlers.py` voice handler wraps the transcribe call in `try: … finally: os.unlink(tmp_path)` with an `OSError` swallow — tempfile is unlinked on every exit path including cancellation. (3) +3 regression tests in `tests/test_voice_i18n.py`: oversize refused with zero network hit, at-cap accepted, missing file returns `voice_error` without crash. (4) New `voice_too_large` key added to `EXPECTED_KEYS` in the i18n parametrised test so future `STRINGS` drift is caught. | **fixed** |
| T076 | voice.py | Security | — | Pass 2 audit of the voice-transcription path (trust-boundary TG-1/DC-1/SK-1 post-download → Whisper upload): OpenAI API key never logged, temp-file cleanup on exception, file-size cap. | **Audit clean after P2-83 is fixed above.** (1) API key: only ever flows via `Authorization: Bearer …` header; logs are `response.text[:200]` (OpenAI body, not our headers) + `str(e)` (httpx exceptions don't stringify request headers) — no leak vector. (2) Temp-file cleanup: now `try/finally` in `handlers.py` so cancellation or any uncaught exception class still unlinks the temp file. (3) Size cap: 25 MB `_MAX_TRANSCRIPTION_BYTES` enforced before network (P2-83 fix). **Adjacent observation (noted, not fixed in T076)**: `bot/messaging/slack.py::download_voice` reads the Slack file body into memory via `resp.content` with no size cap — same class as P2-11 on Discord and P2-82 on Telegram. T072 was previously re-scoped to "no action" but pre-dated the P2-11/P2-82 caps; a Slack cap would complete the pattern. Reopen under a new P2-SEC row when Slack voice features are revisited. Not blocking T076. | fixed |
| P2-84 | memory_store.py:94 (`resolve_db_path`) + memory.py:54 (`get_memory_dir`) | Security | Low | `resolve_db_path` used the specialist ``agent_name`` verbatim as a filename segment (``data_dir / "memory" / f"{agent_name}.db"``). Upstream call sites — `topics.create_specialist`, `topics.create_workspace`, collab Flow B bootstrap — already sanitise via `_sanitize_task_name` (`re.sub(r'[^a-z0-9-]', '-', …)`), so no attacker-reachable traversal vector today. But the migration path `migrations/v0_21_0.py` reads agent names from a hand-editable `state.json` and from pre-existing filesystem subdirectories of `data/memory/`; if an operator ever hand-edits `state.json` with a weird name, or an install is migrated from an older Robyx version that predated sanitisation, an unsanitised name could reach `resolve_db_path`. Same defense-in-depth class as T069's observation about `agent.work_dir` validation at state.json load time. | Added module-level `_validated_db_name_segment()` in `bot/memory_store.py` that rejects empty / `.` / `..` / names containing `/`, `\`, or control chars (matches the `validate_task_name` pattern from `task_runtime.py`). Invoked from the specialist branch of `resolve_db_path` and from the specialist branch of `get_memory_dir` in `memory.py`. Orchestrator/robyx branch and workspace branch (which pins the path to `{work_dir}/.robyx/memory.db` and does NOT bake agent_name into the path) bypass the validator — so the historical workspace-memory rehydration path in `migrations/v0_21_0.py` continues to work on legacy names. +9 regression tests (parametrised traversal matrix + orchestrator-bypass + workspace-bypass + normal-names acceptance). | **fixed** |
| P2-85 | memory_store.py:175 (`search_archive`) | Security | — | Follow-up to an FTS5-injection review: `search_archive` builds `terms = " OR ".join(f'"{w}"*' for w in clean.split())` where `clean = re.sub(r"[^\w\s*]", "", query)`. Quotes, parentheses, backslashes, SQL fragments, FTS5 operators (`AND`/`OR`/`NOT`/`NEAR`), and control bytes are all stripped before double-quoting — but there was no regression test to lock that contract in against future regex loosening. | **No code change required**; audit confirmed the strip + double-quote combo is injection-safe. Added a parametrised regression test matrix (`test_hostile_query_does_not_error`) in `tests/test_memory_store.py` covering: SQL-style noise (`" OR 1=1 --`, `; DROP TABLE`), FTS5 operator strings, path-traversal payloads (`../../etc/passwd`), control bytes (`\x00\x01\x02`), backslashes — every query must return a list (possibly empty) rather than raise. +7 parametrised cases. Ensures any future weakening of the sanitiser surfaces as a test failure. | **noted / test-guarded** |
| T077 | memory.py + memory_store.py | Security | — | Pass 2 audit of the SQLite-backed memory engine: SQLite parameterization, journal-mode safety, FTS5 query injection, path-traversal in `resolve_db_path`, connection leak on exception, secret-in-value leak class. | **Audit clean after P2-84 and P2-85 are closed above.** (1) SQLite parameterization: every `conn.execute` in `memory_store.py` uses `?` placeholders for agent_name, content, topic, tags, reason, terms, limit — no string interpolation into SQL. (2) FTS5 query injection: `search_archive` strip + double-quote pattern is safe (P2-85 adds a regression test matrix). (3) WAL mode is enabled (`PRAGMA journal_mode=WAL`) — crash-safer than rollback journal; each public entry opens + closes a dedicated connection in a `finally` clause (connection-leak audit: all five call sites — `build_memory_context`, `search_memory`, `aggregate_active_summaries`, `migrate_markdown_to_sqlite`, legacy fallbacks — close in `finally`). (4) Path-traversal in `resolve_db_path`: P2-84 closes the defense-in-depth gap. (5) Secret-in-value: memory contents are AI-generated plaintext; the AI could be prompt-injected to store a password in active memory, but that is a user-visible concern (reviewable by the operator) not a code vulnerability. Memory DBs live under `DATA_DIR` (operator-owned) with standard 644 permissions. (6) File-size caps on memory writes: the AI decides what to write — not an attacker-reachable amplification vector. No additional code changes required. | fixed |
| P2-86 | _bootstrap.py:142 (`ensure_dependencies`) | Security | Med | `ensure_dependencies` spawned `pip install -r bot/requirements.txt` via `subprocess.run([str(pip), "install", "-r", str(_REQUIREMENTS)], ..., capture_output=True, text=True, timeout=600)` without an explicit `env=` argument, so the pip subprocess inherited the full parent environment — including platform tokens (`ROBYX_BOT_TOKEN`, `KAELOPS_BOT_TOKEN`, `DISCORD_BOT_TOKEN`, `SLACK_BOT_TOKEN`, `SLACK_APP_TOKEN`) and AI provider keys (`OPENAI_API_KEY`, `ANTHROPIC_API_KEY`). Same class as P2-71 (updater.py), but on the **startup** path: the bootstrap pip install fires whenever `bot/requirements.txt` changes between bot restarts. A malicious `setup.py` in a newly-added transitive dep, a compromised wheel that execs during install, or a `PIP_INDEX_URL`-redirected proxy could read those secrets from `os.environ`. | Added module-level `_CHILD_ENV_SCRUB` frozenset (same 7 keys as `bot/updater.py::_CHILD_ENV_SCRUB`: the 5 platform tokens + 2 AI provider keys) and a stdlib-only `_scrubbed_child_env()` helper at the top of `bot/_bootstrap.py`. `ensure_dependencies` now passes `env=_scrubbed_child_env()` to `subprocess.run`. Operator-set env (`PATH`, `HOME`, `PIP_INDEX_URL`, `HTTPS_PROXY`, cert trust vars) passes through unchanged. Keeps the file's stdlib-only constraint intact. +2 regression tests (`test_pip_subprocess_gets_scrubbed_env` verifies each secret key is stripped and operator env survives; `test_scrubbed_env_returns_dict_without_secrets` asserts the scrub list matches `updater.py` verbatim so divergence surfaces as a test failure). | **fixed** |
| T079 | _bootstrap.py + process.py | Security | — | Pass 2 audit of the startup safety-net (`_bootstrap.py`) and the cross-platform process utility (`process.py`): import-time side effects, subprocess lifecycle guarantees, zombie reaping. | **Audit clean apart from P2-86 (fixed above)**. (1) **Import-time side effects**: both modules are side-effect free at import. `_bootstrap.py` only defines constants + helpers at import; `ensure_dependencies` and `migrate_personal_data_if_needed` are explicitly called from `bot.py`. `process.py` defines a constant tuple and a logger — nothing more. (2) **Subprocess argv safety**: every spawn uses argv lists (no `shell=True`). `process.py` formats `pid` via `%d` / `str(pid)` — `pid` is always an int from internal state (not user-reachable); `_bootstrap.py`'s pip invocation uses `str(pip)` + `str(_REQUIREMENTS)` which are module-derived Path objects. (3) **Timeouts**: `_bootstrap.py` uses a 600 s ceiling on pip; `process.py` uses 5 s on each `ps`/`tasklist` probe. (4) **Zombie reaping**: `subprocess.run` and `asyncio.wait_for(proc.communicate())` both wait for the child and reap — no orphan PIDs. (5) **Exception handling**: every public function in `process.py` catches a broad exception set and returns a safe default (empty string / False). (6) **Env scrub (pip)**: P2-86 fix above. **Low-priority observation (noted, no action)**: `_bootstrap.py` uses `capture_output=True` on pip which buffers the full stdout/stderr in memory before truncating to the last 1 KB at log time; a pathologically verbose wheel install (multi-GB progress bars) could briefly balloon bootstrap memory. Real-world pip output is line-oriented and small; not worth the streaming refactor. | fixed |

---

## Pass 2 SEC-only point-release close-out (2026-04-19)

This Pass 2 cycle ships as a **security-hardening point release** per the
"MVP First: P2-SEC only" option in `plan.md` → Implementation Strategy.
P2-SEC and P2-STB/P2-UX/P2-NI overlap on some modules, but the security
lens is complete and self-contained, so releasing it independently does
not block the remaining phases.

### Shipped in this release (23 findings closed)

All 23 `P2-NN` rows above carry a final status:

| Category | Count | IDs |
|----------|:-----:|-----|
| Security (High) | 2 | P2-10, P2-20 |
| Security (Med) | 11 | P2-11, P2-50, P2-70, P2-71, P2-80, P2-81, P2-82, P2-83, P2-84, P2-86, + P2-12 |
| Security (Low) | 3 | P2-72, P2-73, P2-85 (noted / test-guarded) |
| Stability (Med) | 2 | P2-30, P2-40 |
| Natural-Interaction (Low) | 4 | P2-00, P2-01, P2-02, P2-03, P2-60 (1 Low parity) |

Every module-level audit row (T065, T067–T070, T073, T076–T079, T079a, T081 partial, T085, T088) is recorded above with either `fixed` / `audit clean` / `noted / test-guarded`.

### Deferred to a future Pass 2 cycle (with rationale)

The following Pass 1 deferred findings and Pass 2 non-SEC tasks are
explicitly **deferred** to a subsequent cycle. None of them is a
blocker for the security point release:

| ID / Task | Rationale |
|-----------|-----------|
| **F12** (telegram.py Markdown) | Behavioural-change risk; needs a separate opt-in plan and parse-mode audit across callers. Not a security regression. |
| **F13** (discord.py download_voice error handling) | Error paths are covered by Pass 2 P2-11 (streaming + 25 MB cap); the remaining items are UX polish that belong to P2-UX. |
| **F14** (slack.py reply/edit error handling) | P2-NI scope (tone + retry_send parity), tracked under T111. |
| **F20** (voice.py `%` TypeError) | Covered by P2-60 (i18n substitution test now iterates every key with representative arguments). Reopen only if a new format string slips past the parity test. |
| **All-adapters reply/edit unprotected** | `retry_send` parity is a P2-NI task (T103). |
| **T080** scheduler monotonic time | Deferred with rationale already recorded at T080 in `tasks.md` — needs its own spec because most scheduler timestamps are legitimately wall-clock. |
| **T082–T091** (P2-STB remainder) | Stability audit continues the P2-SEC work on the same modules; shipping SEC first lets operators deploy immediately. |
| **T092–T098** (P2-UX) | User-facing polish; handlers.py gained +703 LOC from 003/004 and needs its own pass. |
| **T102–T108** (P2-NI tone / templates) | String inventory drift after 003/004 needs a full resweep. |
| **T109–T116** (Pass 1 re-evaluations + P1-P5 perf) | Covered either by new P2-NN rows above or by the STB/UX/NI deferrals. No action in this release. |

### Test + LOC delta

| Metric | Start (2026-04-16) | This release | Delta |
|--------|--------------------|--------------|-------|
| Tests (pytest passing) | 1085 | 1532 | +447 |
| LOC under `bot/` | 12 329 | ~14 770 | +~2 440 (includes 003/004) |
| Modules under `bot/` | 53 | 59 | +6 |
| Version | 0.21.0 | **0.22.2** | +0.1.2 (includes 003/004 at 0.22.0 / 0.22.1) |

### Close-out gate (T117)

`pytest tests/ -q` ≥ 1451 required → **1532 passing**, 1 skipped,
zero failures. Gate satisfied.

