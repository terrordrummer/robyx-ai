---
version: 0.20.5
min_compatible: 0.19.0
breaking: false
requires_migration: false
---

## README Overhaul and Kael → Robyx Cleanup

Major documentation pass to align the README with the architecture that
had emerged through the 0.20.x cycle:

- **Replaced** the outdated workspace-type table with a unified
  description that matches the real scheduler model.
- **Rewrote** the Scheduler section to cover all four task types
  (reminders, one-shot, periodic, continuous) in one place.
- **Added** an Agent Interruption section (formalising the behaviour
  introduced in v0.20.2 / v0.20.3).
- **Fixed** the ASCII architecture diagram (`KAEL` → `ROBYX`), the env
  var names, and the listed defaults.
- **Updated** the project structure — removed the retired
  `reminders.py`, added `queue.json`.
- **Replaced** remaining `Kael*` references in code comments so the
  source matches the published product name.

No runtime change; docs-only.

> This file was backfilled retroactively as part of the v0.24.3 doc-alignment
> audit. Content is reconstructed verbatim from the annotated tag message
> at `git tag -l --format='%(contents)' v0.20.5`.
