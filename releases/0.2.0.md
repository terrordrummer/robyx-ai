---
version: 0.2.0
min_compatible: 0.1.0
breaking: false
requires_migration: true
---

## Changes
- Immediate typing indicator (no more 5s delay before showing "typing...")
- Fixed multi-workspace/specialist creation (all commands in a single response now execute)
- Removed voice transcription echo — voice messages go directly to the agent
- Removed periodic status messages during orchestrator processing
- Auto-update system with rollback support (/checkupdate, /doupdate)
- Hourly background update check with one-time-per-version notifications
- Release notes with structured frontmatter (semver, breaking, migration)
- Improved uninstall scripts with full process and state cleanup
- Renamed GitLab repo from kale-ops to kael-ops

## Migration
1. `pip install -r bot/requirements.txt`
