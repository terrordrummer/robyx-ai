---
version: 0.20.1
min_compatible: 0.19.0
breaking: false
requires_migration: false
---

## CHANGELOG Housekeeping

Housekeeping release — no runtime behavior change. `CHANGELOG.md` was
backfilled with structured entries for v0.20.0 (unified scheduler +
continuous tasks) and v0.20.1 itself so the per-version record is
complete going forward.

> This file was backfilled retroactively as part of the v0.24.3 doc-alignment
> audit (the per-release `.md` convention only started being followed
> consistently from v0.20.6). Content is reconstructed verbatim from the
> annotated tag message at `git tag -l --format='%(contents)' v0.20.1`.
